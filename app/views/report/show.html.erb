<h2>Rapportage</h2>
<p>Deze collectie bevat <%= I18n.translate 'count.works', count:  @works_count %>, bestaande uit <%= I18n.translate 'count.inventoried_objects_count', count:  @inventoried_objects_count %>.
  In de rapportage wordt geteld op basis van geïnventariseerde objecten.
</p>
<% if @inventoried_objects_count_in_search != @inventoried_objects_count %>
   <div data-closable class="callout primary">De zoekmachine loopt momenteel achter op de aantallen in de zoekmachine.</div>
<% end%>
<p>
  <%= button_to 'Ververs rapportage', collection_refresh_works_path(@collection), method: :post, class: [:button, :secondary, :small], title: "Afhankelijk van de hoeveelheid werken in dit collectie kan dit soms lang duren." if can? :refresh, @collection%>
</p>
<% begin %>
  <div class="tabs">
    <% if @sections.keys.count > 1 %>
    <nav class="tab-head">
    <ul>
      <% @sections.each do |head, contents| %>
        <li><a href="#<%=head.to_s.parameterize%>"><%= head %></a></li>
      <% end %>
    </ul>
    </nav>
    <% end %>
    <% @sections.each do |head, contents| %>
      <section id="<%=head.to_s.parameterize%>">
        <% if @sections.keys.count > 1 %>

        <h3><%=head%></h3>
        <% end %>
        <div class="row">
          <% contents.each do |column| %>
            <div class="large-<%=(12.0/contents.length).to_i%> column float-left">
              <%= render_report_column(column)%>
            </div>
          <% end %>
        </div>
      </section>
    <% end %>
  </div>
  <p><small>Waar de prijzen oorspronkelijk niet in euro's waren, zijn deze voor de rapportage omgezet naar euro's.</small></p>
<% rescue Elasticsearch::Transport::Transport::Errors::NotFound %>
<p>De rapportage kon niet gedraaid worden</p>
<% end %>