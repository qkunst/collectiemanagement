<%= simple_form_for(@condition) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :order %>
    <%= f.input :hide %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
