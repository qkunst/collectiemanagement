<h2>Overzicht dragers</h2>
<ul>
    <% @media.each do |item| %>
      <li><%= link_to_edit item %> <%=item.hide? ? "(verborgen)" : ""%></li>
    <% end %>
</ul>
<%= link_to 'Nieuwe drager', new_medium_path %>
