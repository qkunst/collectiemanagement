<h1>Registreer</h1>
  <p>Welkom in de <%=I18n.t("application.name")%> webapplicatie. Na registratie zult u geautoriseerd moeten worden door een medewerker van <%=I18n.t("application.responsible_party")%> om toegang te krijgen tot de bedrijfscollectie van uw organisatie. </p>
  <p>Onze omgang met gegevens hebben we beschreven in ons <a href="https://qkunst.nl/privacy/" target="_blank">gegevensbeleid</a>.</p>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, required: true, autofocus: true %>
    <%= f.input :password, required: true, hint: ("(Minimaal #{@minimum_password_length} tekens)" if @minimum_password_length) %>
    <%= f.input :password_confirmation, required: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Registreren", class: :button %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
