<h1>Projectfase: <%= @stage.name %></h1>
<%= render :partial => 'stages_graph', locals: {collection: @collection, action: :link_to} %>

<% if @stage.actual_stage %>
<p>Deze fase is slechts een ontvangende fase om ook convergerende flows mogelijk te maken. Ga naar de eigenlijke fase <strong><%= link_to(@stage.actual_stage.name, @stage.actual_stage) %></strong></p>
<% end %>

<% if @stage.previous_stage %>
<p>Voorgaande fase: <strong><%= link_to(@stage.previous_stage.name, @stage.previous_stage) %></strong></p>
<% end %>

<% if @stage.next_stages %>
<p>Volgende fases: <ul><% @stage.next_stages.collect do |nstage| %>
  <li><%= link_to(nstage.name, nstage) %></li>
<% end %>
</ul></p>
<% end %>
<%= link_to 'Bewerk', edit_stage_path(@stage), class: :button %>