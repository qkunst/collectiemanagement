<h1>Bewerk groepering</h1>
<div class="row">
  <div class="medium-6 column">
    <%= hidden_field_tag name: :collection_id, value: @collection.id if @collection %>
    <%= simple_form_for [@collection, @work_set].compact do |f| %>
      <%= f.error_notification %>
      <ul>
        <% @work_set.errors.each do |a| %>
          <li><%= a.message%></li>
        <% end %>
      </ul>
      <%= f.association :work_set_type%>
      <%= f.input :identification_number %>
      <%= f.submit class: :button%>
    <% end %>
  </div>
  <div class="medium-6 column">

  </div>
</div>
<hr/>
<h3 id="works">Werken</h3>
<p>De volgende werken zitten in deze groepering: </p>
<%= render @works.limit(24) %>
<% if @works.count > 24%>
<p>Dit zijn niet alle werken. <%= link_to "Open een overzicht met alle #{I18n.t("count.works", count: @works.count) }", collection_works_path(@collection, params: {ids: @works.pluck(:id).join(",") }), target: "_blank" if @collection %>.</p>
<% end%>
