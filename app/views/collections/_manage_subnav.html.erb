<nav class="sub-nav hide-for-print" aria-label="Beheer collectie navigatie">
<ul class="menu simple">
  <li>Beheer <%=@collection.name%>:</li>
  <li><%= menu_link_to "Herinneringen", collection_reminders_path(@collection) if can? :read, Reminder %></li>
  <li><%= menu_link_to "Thema's", collection_themes_path(@collection) if can? :read, Theme %></li>
  <% if can? :read, ImportCollection.new(collection: @collection) %>
  <li><%= menu_link_to "Import", collection_import_collections_path(@collection) %></li>
  <% elsif can? :read, SimpleImportCollection.new(collection: @collection) %>
  <li><%= menu_link_to "Import", collection_simple_import_collections_path(@collection) if can? :read, SimpleImportCollection %></li>
  <% end %>
  <li><%= menu_link_to "Rapporten", collection_custom_reports_path(@collection) if can? :review_collection, CustomReport %></li>
  <li><%= menu_link_to "Gebruikers", collection_users_path(@collection) if can? :review_collection_users, @collection %></li>

</ul>
</nav>
