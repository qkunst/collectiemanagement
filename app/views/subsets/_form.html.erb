<%= simple_form_for(@subset) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :hide %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
