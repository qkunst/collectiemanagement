<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= "#{I18n.t("application.name")} - #{@title}" %></title>

    <%= stylesheet_link_tag    "application", media: 'all' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= csrf_meta_tags %>
  </head>

  <body class="<% if offline? %>offline-page <% end %><%= @work_display_form&.display %>">
    <nav class="top-bar" role="navigation" aria-label="Hoofdmenu">
      <div class="top-bar-title">
        <h1>
            <%= image_tag "logo.svg", id: 'logo', alt: I18n.t("application.name")%>
        </h1>
      </div>
    </nav>
    <div class="row">
      <div class="large-12 columns">
        <%= yield %>
       </div>
     </div>
     <footer class="">
       <div class="row">
          <%= yield(:footer)%>
       </div>
     </footer>
  </body>
</html>
